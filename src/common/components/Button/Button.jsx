import React from "react";
import styles from "./Button.module.scss";

// eslint-disable-next-line react/prop-types
function Button({ size, label, color }) {
  return <div className={styles.button}>Create</div>;
}

export default Button;
